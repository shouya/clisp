#include <function.h>

/*
struct Function_ {
  char* args;
  int n_insts;
  char* insts;
};
*/

static int function_parse_arg(const char* 

int function_parse(Function* function,
                   const char* arguments, /* as `(x y)' form */
                   const char* body,      /* as `(+ x y)' form*/
  ) {
}
